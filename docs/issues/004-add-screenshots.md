# Issue Draft -- Add Screenshots

**Title:** Add screenshots to documentation

**Labels:** `documentation`, `enhancement`

## Problem

The README references screenshots in `docs/screenshots/` but none have been added yet. Screenshots would help users understand what the tool looks like in action before installing it.

## Proposed solution

Capture and add the following screenshots:

- `cli-help.png` -- CLI `--help` output in a terminal
- `cli-scan.png` -- CLI proxy validation run with working proxies
- `webui-home.png` -- Web UI homepage showing controls, sources panel, and theme toggle
- `webui-results.png` -- Web UI with live scan results, latency bars, and export options

Update the README screenshot section to display the images once they are committed.

## Acceptance criteria

- [ ] All 4 screenshots exist in `docs/screenshots/`
- [ ] README displays the screenshots (or links to them)
- [ ] Screenshots are reasonably sized (< 500 KB each)
